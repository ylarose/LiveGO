This package showcases a simple HTML page, connecting to a GO backend. the GO backend connects to a voice bot, using the Gemini live 2.5 model, in audio mode.
The GO backend includes a very simple tool (getWeather) to showcase how the voice bot can call internal tools.
